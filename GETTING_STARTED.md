# Getting Started - Stock Watchlist AI\n\n## Quick Start (5 minutes)\n\n### 1. Install Dependencies\n\n**Backend:**\n```bash\ncd backend\nnpm install\n```\n\n**Frontend:**\n```bash\ncd frontend\nnpm install\n```\n\n### 2. Start the Backend\n\n```bash\ncd backend\nnpm start\n```\n\nYou should see:\n```\n✓ Stock Watchlist API running on http://localhost:5000\n```\n\n### 3. Start the Frontend (in a new terminal)\n\n```bash\ncd frontend\nnpm start\n```\n\nThe app will open at `http://localhost:3000`\n\n## What You'll See\n\n### Layout\n\n1. **Header** — App title and API connection status\n2. **Report Queue** (top) — Visually distinct section showing:\n   - Pending reports with progress bars\n   - Completed reports with summaries\n3. **Watchlist** (below) — Your 3 starter stocks:\n   - AAPL (Apple)\n   - GOOGL (Alphabet)\n   - TSLA (Tesla)\n\n### Features to Try\n\n#### Generate Reports\n1. Click \"📊 Generate Report\" on any stock\n2. Select a report type:\n   - **Technical Analysis** — Price patterns, RSI, MACD\n   - **Fundamental Analysis** — P/E, earnings, ROE\n   - **Earnings Impact** — Upcoming earnings & volatility\n   - **Custom Analysis** — Combined metrics\n3. Report appears in the queue with progress bar\n4. Simulated progress increments every 10 seconds\n\n#### Create Alerts\n1. Click \"🚨 View Alerts\" on any stock\n2. Click \"+ Create New Alert\"\n3. Choose an alert type:\n   - **Price Target** — Upper/lower price bounds\n   - **Momentum Shift** — RSI, MACD crossovers\n   - **Reversal Signal** — Candlestick patterns\n   - **Earnings Impact** — Pre-earnings volatility\n4. Configure parameters and save\n5. Toggle alerts on/off with the switch\n\n#### Add Stocks\n1. In the watchlist form, enter:\n   - Symbol (e.g., \"MSFT\")\n   - Company name (e.g., \"Microsoft\")\n2. Click \"Add to Watchlist\"\n3. New stock appears immediately\n\n## Project Structure\n\n```\nstock-watchlist/\n├── backend/\n│   ├── server.js              # Express app entry\n│   ├── package.json\n│   ├── models/\n│   │   └── mockData.js        # All mock stock data\n│   ├── routes/\n│   │   ├── watchlist.js       # Stock endpoints\n│   │   ├── reports.js         # Report queue endpoints\n│   │   └── alerts.js          # Alert config endpoints\n│   └── jobs/\n│       └── scheduler.js       # Background job skeleton\n│\n├── frontend/\n│   ├── public/\n│   │   └── index.html\n│   ├── src/\n│   │   ├── App.jsx            # Main app layout\n│   │   ├── services/\n│   │   │   └── api.js         # HTTP client\n│   │   ├── components/\n│   │   │   ├── Header.jsx\n│   │   │   ├── ReportQueue.jsx\n│   │   │   ├── ReportItem.jsx\n│   │   │   ├── Watchlist.jsx\n│   │   │   ├── WatchlistItem.jsx\n│   │   │   ├── ReportGenerator.jsx\n│   │   │   ├── AlertManager.jsx\n│   │   │   └── AlertEditor.jsx\n│   │   └── styles/\n│   │       ├── global.css\n│   │       ├── app.css\n│   │       ├── watchlist.css\n│   │       ├── reports.css\n│   │       ├── alerts.css\n│   │       └── modal.css\n│   └── package.json\n│\n└── README.md\n```\n\n## API Endpoints (All Working)\n\n### Watchlist\n```\nGET  /api/watchlist              → Get all stocks\nPOST /api/watchlist              → Add stock\nGET  /api/watchlist/:symbol      → Get specific stock\n```\n\n### Reports\n```\nGET  /api/reports/queue          → Get all reports (pending + completed)\nGET  /api/reports/completed      → Get only completed reports\nPOST /api/reports/generate       → Request new report\nGET  /api/reports/:reportId      → Get specific report\n```\n\n### Alerts\n```\nGET  /api/alerts                 → Get all alerts\nGET  /api/alerts?symbol=AAPL     → Get alerts for symbol\nPOST /api/alerts                 → Create alert\nPATCH /api/alerts/:alertId       → Update alert (enable/disable)\nGET  /api/alerts/types           → Get alert type schemas\n```\n\n## Next Steps for Development\n\n### Phase 2: Real Stock Data\n- [ ] Integrate Alpha Vantage or IEX Cloud API\n- [ ] Replace mock prices with real-time quotes\n- [ ] Implement 15-minute delay handling (API limitation)\n- [ ] Add historical data for technical analysis\n\n### Phase 3: AI Report Generation\n- [ ] Connect to OpenAI / Anthropic API\n- [ ] Build prompt templates for each report type\n- [ ] Implement actual background job processing (node-cron, Bull)\n- [ ] Store completed reports in database\n\n### Phase 4: Pattern Detection\n- [ ] Implement RSI, MACD, moving average calculations\n- [ ] Detect chart patterns (hammer, doji, etc.)\n- [ ] Build support/resistance level detection\n- [ ] Real-time alert evaluation\n\n### Phase 5: Persistence & Scaling\n- [ ] Add PostgreSQL database\n- [ ] User authentication & multi-user support\n- [ ] Email notifications for alerts\n- [ ] WebSocket for real-time updates\n- [ ] Deploy to cloud (Heroku, AWS, etc.)\n\n## Integration Checklist\n\n### Stock Data\n- [ ] **Alpha Vantage** (free, 5 calls/min) → https://www.alphavantage.co\n- [ ] **IEX Cloud** (free tier available) → https://iexcloud.io\n- [ ] **Finnhub** (free tier) → https://finnhub.io\n\n### AI Services\n- [ ] **OpenAI API** → https://platform.openai.com\n- [ ] **Anthropic Claude** → https://console.anthropic.com\n- [ ] **Hugging Face** → https://huggingface.co/inference-api\n\n### Job Processing\n- [ ] **node-cron** (simple scheduling) → `npm install node-cron`\n- [ ] **Bull** (job queue with Redis) → `npm install bull redis`\n- [ ] **Celery** (if moving to Python) → `pip install celery redis`\n\n### Database\n- [ ] **PostgreSQL** (recommended)\n- [ ] **Sequelize ORM** (Node.js) → `npm install sequelize pg`\n- [ ] **SQLAlchemy** (Python) → `pip install sqlalchemy psycopg2`\n\n## Troubleshooting\n\n### Backend won't start\n```bash\n# Make sure port 5000 is free\nlsof -i :5000\n\n# Or use a different port\nPORT=5001 npm start\n```\n\n### Frontend won't connect to API\n- Check backend is running: `http://localhost:5000/api/health`\n- Check CORS is enabled in `backend/server.js`\n- Check proxy in `frontend/package.json`: `\"proxy\": \"http://localhost:5000\"`\n\n### npm install fails\n```bash\n# Clear cache and retry\nnpm cache clean --force\nrm -rf node_modules package-lock.json\nnpm install\n```\n\n## Questions?\n\nSee `README.md` for architecture notes and `ARCHITECTURE.md` for technical decisions.\n"
          }
        }
      ]
    }